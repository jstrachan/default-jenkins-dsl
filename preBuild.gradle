defaultTasks 'libs'

repositories {
    jcenter()
}

configurations {
    libs
}

dependencies {
    libs 'io.fabric8:gitrepo-api:2.1.5'
    libs 'org.slf4j:slf4j-api:1.7.5'
    libs 'org.slf4j:slf4j-log4j12:1.7.5'
    libs 'org.springframework:org.springframework.core:3.2.5.RELEASE'
}

task clean(type: Delete) {
    delete 'lib'
}

task libs(type: Copy) {
    into 'lib'
    from configurations.libs
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.2.1'
}
